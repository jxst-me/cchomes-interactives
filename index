<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Smart Coastal Layout Engine</title>
<meta name="viewport" content="width=device-width, initial-scale=1">

<style>
  body {
    margin: 0;
    background: #f1f5f9;
    font-family: system-ui, sans-serif;
    color: #0f172a;
    padding: 20px;
  }

  .scx2-wrapper {
    max-width: 1100px;
    margin: 3rem auto;
  }

  /* HEADER */
  .scx2-header {
    position: relative;
    overflow: hidden;
    border-radius: 20px;
    padding: 2.2rem 1.8rem 1.8rem;
    background: radial-gradient(circle at top left, #0ea5e9 0, #0f172a 60%, #020617 100%);
    color: #e5f6ff;
    margin-bottom: 2rem;
  }
  .scx2-header-bg {
    position: absolute;
    inset: 0;
    background-image:
      radial-gradient(circle at 15% 20%, rgba(255,255,255,0.18) 0, transparent 50%),
      radial-gradient(circle at 80% 30%, rgba(56,189,248,0.35) 0, transparent 55%);
    mix-blend-mode: screen;
    opacity: 0.7;
  }
  .scx2-title {
    position: relative;
    margin: 0;
    font-size: 2rem;
    margin-bottom: 0.4rem;
  }
  .scx2-subtitle {
    position: relative;
    font-size: .98rem;
    max-width: 540px;
  }

  /* MAIN LAYOUT */
  .scx2-main {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
  }

  @media(max-width: 900px) {
    .scx2-main {
      grid-template-columns: 1fr;
    }
  }

  /* BLUEPRINT CARD */
  .scx2-blueprint-card {
    background: #ffffff;
    border-radius: 20px;
    padding: 1.5rem;
    border: 1px solid #e2e8f0;
    box-shadow: 0 10px 30px rgba(15,23,42,0.08);
  }

  .scx2-chip {
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    padding: 0.25rem 0.6rem;
    border-radius: 999px;
    background: #0f172a;
    color: #e5f6ff;
  }

  .scx2-chip-soft {
    background: #e0f2fe;
    color: #0f172a;
  }

  .scx2-blueprint {
    background: #0b1220;
    border-radius: 18px;
    padding: 0.75rem;
    margin-bottom: 0.75rem;
  }

  .scx2-svg {
    width: 100%;
  }

  /* Blueprint shapes */
  .scx2-level rect {
    fill: #020617;
    stroke: #38bdf8;
    stroke-width: 1.5;
    transition: 0.25s ease;
    cursor: pointer;
  }
  .scx2-level:hover rect {
    fill: #0c4a6e;
    stroke-width: 2;
  }

  .scx2-zone {
    fill: rgba(15,23,42,0.7);
  }

  .scx2-level-label {
    font-size: 8px;
    fill: #e5f6ff;
    text-anchor: middle;
  }

  .scx2-stairs rect {
    fill: transparent;
    stroke: #64748b;
    stroke-dasharray: 3 2;
  }
  .scx2-stairs-label {
    font-size: 7px;
    fill: #64748b;
    text-anchor: middle;
  }

  /* Legend */
  .scx2-legend {
    border-radius: 14px;
    background: #f8fafc;
    padding: 0.9rem 1rem;
    border: 1px dashed #cbd5e1;
  }
  .scx2-legend-title {
    font-weight: 600;
    margin-bottom: 0.5rem;
    display: block;
  }
  .scx2-legend-list {
    font-size: 0.85rem;
    margin: 0;
    padding-left: 1.2rem;
    color: #475569;
  }

  /* MODE NAV */
  .scx2-mode-nav {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.6rem;
  }
  .scx2-mode-btn {
    background: #f8fafc;
    padding: 0.65rem 0.9rem;
    border-radius: 999px;
    border: 1px solid #e2e8f0;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 0.7rem;
    transition: 0.25s;
  }
  .scx2-mode-btn:hover {
    transform: translateY(-2px);
  }
  .scx2-mode-btn--active {
    background: #0ea5e9;
    color: #fff;
    border-color: #0284c7;
    box-shadow: 0 6px 18px rgba(14,165,233,0.45);
  }
  .scx2-mode-icon {
    font-size: 1.4rem;
  }
  .scx2-mode-text small {
    font-size: .78rem;
    opacity: 0.85;
  }

  /* PANEL CONTENT */
  .scx2-panels {
    background: #ffffff;
    border-radius: 16px;
    padding: 1.2rem;
    border: 1px solid #e2e8f0;
    box-shadow: 0 6px 20px rgba(15,23,42,0.06);
    margin-top: 0.5rem;
  }
  .scx2-panel {
    display: none;
    animation: fade 0.3s ease;
  }
  .scx2-panel--active {
    display: block;
  }

  @keyframes fade {
    from { opacity:0; transform:translateY(8px); }
    to { opacity:1; transform:translateY(0); }
  }

  .scx2-panel h3 {
    margin-top: 0;
  }

  .scx2-panel-desc {
    font-size: .88rem;
    color: #475569;
  }

  .scx2-panel-list {
    padding-left: 1.2rem;
  }

  /* Assistant Bubble */
  .scx2-assistant {
    margin-top: 0.8rem;
    display: flex;
    gap: 0.7rem;
    padding: 0.7rem 0.8rem;
    background: #ecfdf5;
    border-radius: 14px;
    border: 1px solid #bbf7d0;
  }
  .scx2-assistant-avatar {
    background: #22c55e;
    color: white;
    width: 32px;
    height: 32px;
    border-radius: 999px;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  /* Bottom Strip */
  .scx2-strip-scroll {
    display: flex;
    gap: 0.6rem;
    overflow-x: auto;
    padding-bottom: 0.5rem;
  }
  .scx2-strip-card {
    min-width: 150px;
    background: #f8fafc;
    border: 1px solid #e2e8f0;
    padding: 0.6rem;
    border-radius: 12px;
  }

</style>
</head>

<body>

<div class="scx2-wrapper" id="smart-coastal-widget">

  <!-- HEADER -->
  <div class="scx2-header">
    <div class="scx2-header-bg"></div>
    <h2 class="scx2-title">Smart Coastal Home Layout Engine</h2>
    <p class="scx2-subtitle">Explore how one Myrtle Beach home layout adapts for floods, aging, rentals, and resale.</p>
  </div>

  <div class="scx2-main">

    <!-- LEFT: BLUEPRINT -->
    <div cla
